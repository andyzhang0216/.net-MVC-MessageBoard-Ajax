@model IPagedList<Messages>

@if (Model != null)
{
    foreach (var item in Model)
    {
        <table class="message">
            <tr>
                <td class="title">@item.Title</td>
            </tr>
            <tr>
                <td class="time">@item.Time.ToString("yyyy/MM/dd hh:mm")</td>
            </tr>
            <tr>
                <td class="content">
                    @item.Content
                </td>
            </tr>
        </table>
    }
}
@Html.PagedListPager(Model, page => Url.Action("RenderPosts", new { page }), PagedListRenderOptions.EnableUnobtrusiveAjaxReplacing(new AjaxOptions() { UpdateTargetId = "messages_block" }))
